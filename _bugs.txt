space midline, then backspace midline, line gets cut off and can't cycle between rest of visible characters
pressing space while on an existing character doesn't adjust the buffer
history file loading into memory
delete key doesn't work
